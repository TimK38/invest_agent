# 使用場景清單與驗證規格

**每次調整策略（改 `STRATEGY.md` 門檻、改 `profiles/*.json` 參數、改任何腳本的判斷邏輯）
之後，一律執行：**

```bash
./envest_agent/bin/python verify_scenarios.py
```

它跑的是**真正的 CLI**，不是重新實作一份判斷邏輯——重新實作等於自己跟自己對答案，
測不出工具本身的 bug。這份文件是規格，`verify_scenarios.py` 是它的可執行版本。

> **實績**：
> 1. 第一次跑就抓到 `buy_check.py` 把 B1 當成 B2、在「大盤跌破 SMA20」時仍放行現股新開倉
>    ——那正是「A 策略打 B 策略」的實例。
> 2. 補完 S14 之後，它抓到我自己改壞的斷言（把「加碼空間」誤判成逐檔動作），
>    以及輸出格式改動導致的舊斷言失效。**驗證套件也會驗證驗證套件。**
> 3. 補 S11 時發現**斷言有洞**：看到單位是「張」就直接放行，於是刻意把
>    無條件捨去改回四捨五入，測試竟然全過。改為直接測 `lots_txt` 純函式的八個邊界值，
>    同樣的迴歸立刻被抓出來。**測不到迴歸的測試等於沒有測試——每個新斷言都該故意改壞驗一次。**
>
> **臨時設定檔**：本套件會建立 `profiles/_verify.json` 與 `_verify_positions.json`，
> 用固定的淨資產與持股跑測試，**跑完自動刪除**（`try/finally`）。
> 這樣測試結果不會隨你今天買了什麼而變。

---

## 一、狀態閘門規格（`verify_scenarios.py` 的 `GATE` 表）

**這張表就是規格本身。改策略時先改它，再改程式，然後跑驗證。**

| 大盤狀態 | 定義 | 現股新開倉 | 融資新開倉 | 持股動作 |
|---|---|---|---|---|
| **A** | 站上 SMA20 + 多頭排列 + ATR% ≤ 2.0 | ✅ 允許 | ✅ 允許 | §6 兩軸線 |
| **B2** | 站上 SMA20，但未達 A | ✅ **§3 例外**：現股首波 1/3 | ❌ 不新增 | §6 兩軸線 |
| **B1** | **跌破** SMA20（仍在 SMA60 上） | ❌ 只出不進 | ❌ | §6 兩軸線 |
| **C** | 破 SMA60／波動警訊／距高 ≤ -8% | ❌ | ❌ **融資須歸零** | 降曝險至上限內 |
| **D** | 破 SMA120／距高 ≤ -12% | ❌ | ❌ | 全部現金 |

**B1 與 B2 是最容易搞混的一對**：兩者在工具裡都印「B」，但對進場的意義完全相反。
`§7` 的再進場訊號要求「站上 SMA20」，**在定義上只可能發生在 B2**。

---

## 二、使用情境清單

### 你提出的（S1~S4）

| # | 情境 | 對應模式 | 工具 | 驗證狀態 |
|---|---|---|---|---|
| S1 | **收盤前**問持股狀況（13:20~13:25） | B | `portfolio_check.py` | ✅ 已驗 |
| S2 | **收盤後** review（15:00 後） | B | `market_state` + `portfolio_check` | ✅ 已驗 |
| S3 | **盤中**問買個股（現股／融資） | A | `buy_check.py` | ✅ 已驗 |
| S4 | **盤中**問買 ETF（現股／融資） | A | `buy_check.py` | ✅ 已驗 |

### 你沒提到、但同樣會發生的

| # | 情境 | 為什麼重要 | 驗證狀態 |
|---|---|---|---|
| S5 | **賣出檢查**：規則觸發 vs 情緒 | 過早賣的代價 << 不停損，不可一律阻止 | ⬜ 待補 |
| S6 | **加碼既有部位** | ≠ 新開倉。A 狀態才可加碼，且**只能加在獲利部位**（鐵則 1） | ✅ 已驗 |
| S7 | **「我是不是賣飛了」** | 模式 C-2，先判真／假賣飛再談回補 | ⬜ 待補 |
| S8 | **狀態當日轉換**（A→B→C→D） | §3：降槓桿必須當日或次一交易日完成 | ✅ 已驗 |
| S9 | **部位漲上去後被動超標** | 沒有新交易也會違規，需要減碼 | ✅ 已驗 |
| S10 | **§7 連續停損 2 次禁交易** | `positions.py`／`buy_check.py` 都要擋。**四種組合都測**：該擋的擋、不該擋的不能誤擋 | ✅ 已驗 |
| S11 | **不足 1 張（零股）** | 高價股常見；四捨五入曾把 0.65 張印成「1 張」而那 1 張過不了檢查 | ✅ 已驗 |
| S12 | **上櫃股**（如 6182、8096） | TWSE 抓不到要改抓 TPEx，資料也較晚 | ✅ 已實作 |
| S13 | **停牌／減資／分割標的** | 空洞會被誤判成停牌，污染報酬 | ✅ 已實作缺漏檢查 |
| S14 | **多檔同時觸發不同動作** | 需有優先序：破停損 > 破 SMA20 > **單一標的超標** > 破 EMA8 > 鎖利 | ✅ 已驗 |
| S15 | **融資追繳逼近** | 追繳是吸收態，不是帳面虧損。驗融資才印、現股不印、公式 = 成本×1.30×成數 | ✅ 已驗 |
| S16 | **§4 資產結構偏離** | 穩健／積極偏離 ±10pp 才調整 | ✅ 已實作 |
| S17 | **空手時問「現在可以進場嗎」** | 需同時檢查大盤層級與個股層級 | ✅ 已驗 |
| S18 | **週末／非交易日／盤中提問** | 資料停在最後交易日，要標明而非假裝即時 | ✅ 已驗 |
| S19 | **標的完全沒資料** | 要先 `fetch_stocks.py` 再 `clean_stocks.py` | ✅ 已實作 |
| S20 | **盤中破 SMA20 但尾盤拉回** | §6 是**收盤**判定；實測假訊號率大盤 15%、個股平均 16%（00981A 34%） | ✅ 已驗 |

### S18 的四種時點（`freshness()` 純函式，已納入驗證）

| 情況 | 判定 | 輸出 |
|---|---|---|
| 資料日 = 今天 | ✅ 可視為當日 | 「當日收盤資料」 |
| 今天是交易日但**尚未收盤** | ⚠ | 「今天 X 尚未收盤（現在 HH:MM）→ 以下依 Y 收盤判定，不是今天的盤」 |
| 今天是**週末** | ⚠ | 「今天 X（週六/日）非交易日 → 以下依 Y 收盤判定」 |
| 今天已收盤但**資料只到更早** | ⚠ | 「可能是假日，也可能是資料未更新 → 先跑 fetch」 |

三支工具（`market_state` / `portfolio_check` / `buy_check`）都會印，驗證會確認沒有任何一支漏掉。

### S20 的盤中警語

盤中或非交易日執行 `portfolio_check` 時，逐檔動作前會先印：

```
⚠ 以下動作依 2026-08-03 **收盤**判定，不是即時的。
  §6 是收盤判定規則。**盤中假訊號率**（盤中跌破 SMA20 但收盤守住）：
  大盤 15%、個股平均 16%（00981A 高達 34%）——照盤中訊號賣會被洗掉。
  要在今天執行出場，請於 13:20~13:25 用當下價格再判一次。
```

假訊號率是本專案資料實測（2023-07~2026-08，大盤 268 次盤中跌破中 39 次收盤守住）。

---

## 三、跨規則衝突檢查（「A 策略打 B 策略」）

這些是**兩條規則同時適用時**必須有明確優先序的地方：

| 衝突 | 目前的解法 | 出處 |
|---|---|---|
| §3「B 只出不進」vs §7「訊號成立建 1/3」 | 例外只適用 **B2**，B1 不適用 | §3 v1.6 |
| §6 軸線一（目標鎖利）vs 軸線二（均線防守） | **取先觸發者**；同日多條觸發取最嚴重 | §6 |
| 大盤 C/D 降曝險 vs 個股仍在 SMA20 上 | **大盤層級優先**，不論個股狀態 | §6 軸線二 |
| §3 槓桿上限 vs 自備款上限 | 取先綁住者；`portfolio_check` 會指出是哪一條 | §4 v1.6 |
| §3 槓桿上限 vs 單一標的上限 | **單一標的上限永遠較嚴**，不隨槓桿放大 | §3 v1.9 |
| §2 波動警訊 vs §7 再進場訊號 | v1.7 後互斥（警訊需收盤 < SMA20，訊號需 > SMA20） | §2 v1.7 |

---

## 四、每次改策略的流程

```bash
# 1. 改 STRATEGY.md / profiles / 腳本
# 2. 更新本文件的規格表（若閘門行為有變）
# 3. 跑驗證
./envest_agent/bin/python verify_scenarios.py

# 4. 跑既有回測，確認門檻在新資料上仍成立
for f in research/analyze*.py; do ./envest_agent/bin/python "$f"; done
```

**驗證失敗就不要 commit。** 這份清單的價值不在跑過一次，而在每次改動都跑。

### 寫新斷言時的自檢

加一條新斷言之後，**故意把對應的程式改壞，確認測試真的會紅**。
會過的測試不代表程式是對的，只代表這條斷言可能根本沒在測東西。
純邏輯（捨入、時點判定、公式）優先寫成**可直接呼叫的純函式測試**，
比從輸出字串撈正則穩定得多——`lots_txt`、`freshness` 都是這樣測的。
